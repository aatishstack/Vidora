{
  "name": "vidora-backend",
  "version": "2.0.0",
  "description": "Enterprise Video Extraction and Dynamic Format Resolution Pipeline for Vidora Client",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/app.js",
    "dev": "ts-node-dev --respawn --transpile-only src/app.ts",
    "lint": "eslint 'src/**/*.ts'"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "ioredis": "^5.4.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.12",
    "ts-node-dev": "^2.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
